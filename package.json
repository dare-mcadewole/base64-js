{
  "name": "base64",
  "version": "1.0.0",
  "description": "A Library to perform base64 encoding",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore node_modules/,./package.json,./.babelrc,./package-lock.json --copy-files",
    "dev": "npm run build && node dist/"
  },
  "keywords": [],
  "author": "Dare McAdewole <dare.dev.adewole@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "logger": "0.0.1"
  }
}
